{
  "name": "gutu-lib-ui-table",
  "private": true,
  "type": "module",
  "workspaces": [
    "framework/libraries/*"
  ],
  "scripts": {
    "build": "bun run --cwd framework/libraries/ui-table build",
    "typecheck": "bun run --cwd framework/libraries/ui-table typecheck",
    "lint": "bun run --cwd framework/libraries/ui-table lint",
    "test": "bun run --cwd framework/libraries/ui-table test",
    "test:unit": "bun run --cwd framework/libraries/ui-table test:unit",
    "docs:summary": "node scripts/docs-summary.mjs",
    "docs:check": "node scripts/docs-check.mjs"
  }
}
